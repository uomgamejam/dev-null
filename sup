#include <list>

super{
  
  std::list<Platform> platforms; 
  Player player
  
  
  void update(){
    
    if (platforms(0).getX() + platforms(0).getLength() < 0)
      platforms.pop_front();
      
    if (platforms.
      
  }

  void createPlatform(){
    
    Platform newPlatform = new Platform();
    platforms.push_back(newPlatform);  
    
  }

}
